
#include "char_helper.h"





namespace CHAR_HELPER
{
    int get_index_of(char* pStringArray, char Element, Uint8 Length)
    {
        int found = -1;
        /*Uint8 i;

        for(i = 0u; i < Length; i++)
        {
            if(pStringArray[i] == Element){
                return i;
            }
        }*/


        return found;
    }
}